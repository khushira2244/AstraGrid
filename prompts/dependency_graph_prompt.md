# AstraGrid Infrastructure Dependency Graph Prompt

Use AstraGrid outputs only.

Start from the Infrastructure Dependency Graph scenario.

Explain why the dependency graph supports the POWER_TO_WATER_CASCADE conclusion.

Include:
1. what dependency was checked,
2. the power asset,
3. the water asset,
4. the relationship between them,
5. the dependency tool name,
6. the tool run ID,
7. why this dependency makes the cascade hypothesis stronger than the direct-water-attack hypothesis,
8. how this supports safe autonomous response planning.

Do not modify evidence files.
Do not invent evidence.
Do not claim attacker country attribution.
Do not recommend destructive infrastructure actions.

Keep the answer concise and judge-facing.